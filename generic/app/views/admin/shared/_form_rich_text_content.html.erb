<% if object.class.name.constantize.reflect_on_association(:rich_text_content).present? %>
  <%= f.label t 'content' %>
  <div class="content-area">
    <%= f.rich_text_area :content %>
  </div>
<% end %>